# REV 03 — deltas vs REV 02 (source: claude.ai artifact "PARRIT / OS — Brand Command Center", pasted by Paul 14/08 ~23h)

The full REV 03 HTML source is the artifact Paul was shown ("l'artifact de base").
The repo's parrit-command-system-rev02.jsx was an EARLIER revision — the conformity
brief (CONFORMITY-REV01.md) was derived from REV 02 and is superseded on these points.
Scope for the SITE = view "02 / Website" (+ shared primitives). Views Cockpit/Super
App/Dossier/Institution/Rationale = presentation material, NOT site pages.
SILVANI GROUP remains banned in prod (neutral relabel stands).

## Deltas applied to production (14/08 night)
1. RED LAW on CTAs: `.btn.exec` = filled red (bg --red, border --red, white text,
   hover --red-p). Hero primary CTA "Commission your Operating System" and /standard
   CTA become exec-red. Ghost/outline unchanged. (REV 02 brief said "no filled red" — REV 03 overrides.)
2. `.k` letter-spacing: .16em → .18em.
3. Hero: padding 96px 0 72px · h1 clamp(40px, 6.8vw, 88px) · p 16px, margin-top 28px · CTA row margin-top 36px.
4. Instrument object: max-width 760px · bar padding 13px 18px · row padding 17px 18px ·
   value 26px / min-width 88px · row label #C7CBCF 13.5px · tag 9.5px .12em.
5. Operating loop: 5-track grid (1fr auto 1fr auto 1fr) with "→" arrow cells,
   1px separators; REV 03 copy (shorter):
   Understand: "The system maintains a live model of the company: orders, cash, operations, people, clients."
   Decide: "Only what requires the executive reaches the executive. Framed, sourced, quantified."
   Act: "A decision executes through the same system that surfaced it. Traced, reversible, journaled."
   Mobile: stack, arrows rotate.
6. Commissioning cells: h4 17px.
7. Standard doc: border 1px solid --ink (not 1.5) incl. head/foot rules · head padding
   18px 22px, font 11px .16em · rows grid 92px / 170px / 1fr · PS-04 wording:
   "…before it is put into production." (REV 03) — replaces "…before it enters production."
8. Playwright lock updated: home H1 = 88px ±2 at 1440.
9. Buttons type: Geist Mono 10.5px .16em uppercase, padding 12px 18px (all variants).

## NOT applied (presentation-only or awaiting Paul)
- Boot/Opening view ("The system your company [operates] on.") — presentation opener, not a site page.
- The Parrit Frame corner-bracket device on site elements — REV 03 uses it in cockpit/
  mobile/dossier (decision objects); the site view 02 does not carry it except the boot hero. Not added.
- Cockpit / Super App / Dossier / Institution views — product & sales artefacts.
- R-04 open decisions (definitive red, licensed type pair, motion spec) — Paul.
